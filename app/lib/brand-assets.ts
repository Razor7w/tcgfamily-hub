/** Rutas públicas de marca SVG (ver `public/brand/`). */
export const BRAND_LOGO_SRC = '/brand/tcg-nexo-logo.svg'
export const BRAND_MARK_SRC = '/brand/tcg-nexo-mark.svg'

/** Vista previa social (raster 1200×630; los crawlers no usan SVG). */
export const BRAND_OG_IMAGE_SRC = '/brand/og-default.png'

/** Proporción del lockup horizontal (viewBox 296×64). */
export const BRAND_LOGO_ASPECT = 296 / 64
