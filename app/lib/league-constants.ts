/** Puntos por posición legados en el modelo (ya no usados en el cálculo de la liga). */
export const DEFAULT_LEAGUE_POINTS_BY_PLACE = [12, 10, 8, 6, 4, 3, 2, 1];

/** Puntos de liga por partido según récord del participante en el torneo (TDF). */
export const LEAGUE_SCORE_WIN = 3;
export const LEAGUE_SCORE_LOSS = 0;
export const LEAGUE_SCORE_TIE = 1;
